-To get the id of a document, collection, or whatever one must copy it from the address bar, I found no other way to get them so far, for example:

the https://gemis-dev.getoutline.com/doc/assorted-tips-LnATtbMpC5 is this documents link, the document id is LnATtbMpC5 , if I want to access it via the api I must copy it from here first apparently

-Only API keys with no restrictions seem to allow me to access collections or documents, I don’t manage to get past the conn = http.client.HTTPSConnection("gemis-dev.getoutline.com") in my pyhon code because it says I don’t have permission, if i add the full link of the document because it throws an error conn = http.client.HTTPSConnection("gemis-dev.getoutline.com/doc/assorted-tips-LnATtbMpC5gemis-dev.getoutline.com").

-Aparentemente hay un buffer con una versión anterior del documento en algún server, por eso pasan cosas como que desde el editor de outline elimino todo el contenido de un archivo, espero un tiempo, ejecuto el código que usa la api para agregar una línea al documento y no sólo agrega la línea, sino que además restituye todas las líneas que había borrado desde el editor en la web. La solución a eso para las purebas parece ser darle la instrucción de que append = False al ejecutar el código en python así borra todo lo anterior en el documento y queda sólo el contenido enviado por la api. Después hay que restituir el append = True para que no pise el contenido anterior y sólo agregue lineas al final del documento.(El problema es verificado comparando jsons de un documento modificado desde la web que después el impreso por terminal, a continuación se modifica mediante la api y se pide imprimir el resusltante, la inconsistencia queda en evidencia)

-El webhook se deshabilita solo, el mail de ellos dice : “Your webhook (Updates_to_files_sent_to_google_sheets) has been automatically disabled due to a high failure rate in recent delivery attempts. You can re-enable by editing the webhook.“ pero no encuentro logs de errores. Por ahora se soluciona volviendo a habilitar el webhook desde la página, pero es muy poco práctico y poco viable para un buen caso de uso de automatización. Aclaración: esto me viene pasando cuando mando updates a documentos desde la API, la edición manual de los documentos no parece provocar el mismo problema.
